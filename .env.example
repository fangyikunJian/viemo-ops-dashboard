# Copy this file to `.env` before running the application.
#
#   cp .env.example .env
#
# The default is a local SQLite file at the repository root. It is created by
# `npm run setup` and is gitignored — every developer has their own, and nobody
# needs to install a database server.
DATABASE_URL="file:./dev.db"
